# Swagger file for the server

Once the server is up and running, the swagger will be available to the following URL
```
<ec2 ip address>/docs
```
Alternatively, the swagger is available in the repository (openapi.json)
